# フィールド・ショップ・ガルウィング修正 v66

- 町の residentPet を通常のNPCと同じ当たり判定に含め、移動中の現在地・移動先を予約。自由移動とマス移動の双方に対応。
- 住人の連れ歩きガオンを描画だけの追加オブジェクトから実際のNPCへ変更。安全な初期位置、飼い主からの距離、経路探索、他NPC・主人公との衝突を使い、通常の奥行き順に描画。
- 主人公の追従間隔を画像サイズから計算。ガルウィングは約2.29マス。曲がり角では経路上の距離だけでなく実際の距離も確認する。
- ショップの名前・価格を同じ行へ配置。所持数・操作説明も枠の内側に移動。
- ガルウィング（種族番号20）の敵・味方のバトル画像を88→112pxに変更し、足元の位置を維持。

検証: node tools/verifyFieldFixesV66.cjs（localhost:5182）。10エリアのペット衝突、移動先予約、NPC同士の同位置への侵入、曲がり角の追従間隔、ショップ・フィールド・バトルの実画像を確認。生成画像の変更はなく、既存素材の表示と位置の修正。
