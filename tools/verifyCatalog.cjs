const {chromium}=require('C:/Users/voraz/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('assert/strict');(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});const p=await b.newPage({viewport:{width:390,height:844}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto((process.argv[2]||'http://127.0.0.1:5179')+'/gaon-zukan/');assert.equal(await p.locator('article').count(),153);await p.evaluate(()=>Promise.all([...document.images].map(im=>{im.loading='eager';return im.decode()})));assert.ok(await p.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));await p.locator('#search').fill('ラテット');assert.equal(await p.locator('article:visible').count(),1);await p.locator('#search').fill('リーフィン');assert.equal(await p.locator('article:visible').count(),3);await p.locator('#search').fill('存在しない');assert.equal(await p.locator('article:visible').count(),0);await p.locator('#search').fill('');assert.equal(await p.locator('article:visible').count(),153);assert.equal(await p.locator('link[rel=canonical]').getAttribute('href'),'https://midori-no-shima.vercel.app/gaon-zukan/');await p.screenshot({path:'artifacts/public-catalog-mobile.png'});assert.deepEqual(errors,[]);console.log('PASS: 153 images, mobile overflow, searches, reset, canonical and console');await b.close()})().catch(e=>{console.error(e);process.exit(1)});
