const fs=require('fs'),http=require('http'),path=require('path'),assert=require('assert/strict');
const root=path.resolve(__dirname,'..'),L=require(root+'/api/_lib'),F=require(root+'/api/_friendStore');
const admin={id:'ヤノケン',created:1789724902905,display:'ヤノケン'},ordinary={id:'tester',created:1};L.setPw(admin,'test-only-password');let docs={};L.readUser=async id=>id===admin.id?admin:id==='tester'?ordinary:null;L.findUser=async name=>name===admin.id?admin:null;L.configured=()=>true;F.read=async keys=>Object.fromEntries(keys.filter(k=>docs[k]).map(k=>[k,structuredClone(docs[k])]));F.commit=async(expected,writes)=>{if(Object.entries(expected).some(([k,v])=>(docs[k]?.version||0)!==v))return false;for(const[k,v]of Object.entries(writes))docs[k]={version:(docs[k]?.version||0)+1,data:structuredClone(v)};return true;};
const handler=require(root+'/api/map-editor'),auth=require(root+'/api/auth');
const mime={'.js':'text/javascript','.mjs':'text/javascript','.json':'application/json','.css':'text/css','.html':'text/html','.png':'image/png','.webp':'image/webp','.mp3':'audio/mpeg'};
const server=http.createServer(async(req,res)=>{res.status=c=>{res.statusCode=c;return res};res.json=v=>{res.setHeader('Content-Type','application/json');res.end(JSON.stringify(v))};const url=new URL(req.url,'http://localhost');if(url.pathname.startsWith('/api/')){let body='';for await(const b of req)body+=b;try{req.body=JSON.parse(body||'{}');}catch{req.body={};}if(url.pathname==='/api/map-editor')return handler(req,res);if(url.pathname==='/api/auth')return auth(req,res);return res.status(200).json({});}let file=path.join(root,decodeURIComponent(url.pathname));if(!file.startsWith(path.normalize(root)))return res.status(403).end();try{if(fs.statSync(file).isDirectory())file=path.join(file,'index.html');res.setHeader('Content-Type',mime[path.extname(file)]||'application/octet-stream');fs.createReadStream(file).pipe(res);}catch{res.status(404).end();}});
(async()=>{await new Promise(r=>server.listen(5183,'127.0.0.1',r));const{chromium}=require('C:/Users/voraz/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),browser=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});try{const p=await browser.newPage({viewport:{width:1400,height:1000}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:5183/map-editor/');await p.locator('#password').fill('test-only-password');await p.locator('#loginForm button').click();await p.waitForFunction(()=>!document.getElementById('app').hidden&&!document.querySelector('.layout').inert);
const results=await p.evaluate(async()=>{const M=await import('/src/editorModel72.mjs'),{extendMaps82}=await import('/src/customMaps82.mjs'),{selectionInfo90}=await import('/map-editor/selectionInfo90.js'),maps=extendMaps82(await fetch('/assets/editor-v72/base-maps.json').then(r=>r.json())),cat=M.catalog(maps,[]),base=maps.rods,doc=M.initial(base),rendered=M.applyEdit(base,doc,cat),info=(kind,item)=>selectionInfo90({selection:{kind},item,base,rendered,doc,cat,maps,people:{}}),check=(v,msg)=>{if(!v)throw Error(msg);};const buildings=doc.objects.filter(o=>M.objectSource(base,o,cat)?.door).map(o=>({item:o,info:info('object',o)}));check(buildings.some(b=>/家/.test(b.info.title)),'house destination name');const ground=info('ground',{x:15,y:15});check(ground.title&&!ground.title.includes('undefined'),'native floor');const actor=doc.actors.find(a=>!M.actorSource(base,a,cat).artMon);check(info('actor',actor).title===M.actorSource(base,actor,cat).displayName||info('actor',actor).title===M.actorSource(base,actor,cat).name,'person name');const pet={id:'a:pet90',type:'pet',species:'コケゴロ',name:'こけちゃん',x:12,y:14,mode:'follow',owner:actor.id};doc.actors.push(pet);check(info('actor',pet).lines.some(s=>s.startsWith('飼い主：')),'pet owner');check(info('actor',pet).title==='こけちゃん','pet nickname');const tile=info('tile',{material:'pond87-rods-tl',x:4,y:4});check(tile.title.includes('左上'),'floor material');const house=buildings.find(b=>/家/.test(b.info.title));return {house:{x:house.item.x+1,y:house.item.y+1,title:house.info.title},person:{x:actor.x,y:actor.y,title:info('actor',actor).title}};});
const click=async(x,y)=>p.locator('#mapCanvas').evaluate((c,{x,y})=>{const r=c.getBoundingClientRect();c.setPointerCapture=()=>{};for(const type of ['pointerdown','pointerup'])c.dispatchEvent(new PointerEvent(type,{bubbles:true,pointerId:1,pointerType:'mouse',button:0,clientX:r.x+(x+.5)*32*r.width/c.width,clientY:r.y+(y+.5)*32*r.height/c.height}));},{x,y});
await click(results.house.x,results.house.y);assert((await p.locator('#selectedInfo90').textContent()).includes(results.house.title));assert((await p.locator('#selection').textContent()).includes('室内・行き先：'));await p.screenshot({path:root+'/work/admin72/screens/info-house90.png'});await click(results.person.x,results.person.y);assert((await p.locator('#selectedInfo90').textContent()).includes(results.person.title));await click(15,15);assert((await p.locator('#selectedInfo90').textContent()).includes('元からある地面・床'));const before=await p.locator('#dirty').textContent();await p.locator('#mapCanvas').press('ArrowRight');await p.locator('#rotateRight81').click();assert.equal(await p.locator('#dirty').textContent(),before,'inspection mutated floor');await p.locator('[data-category=props]').click();await p.locator('#search').fill('広葉樹');await p.locator('#materials button[data-key=tree]').click();assert((await p.locator('#selectedInfo90').textContent()).includes('広葉樹'));assert(!(await p.locator('#selectedInfo90').textContent()).includes('：tree'));await p.locator('#selectTool').click();await p.setViewportSize({width:390,height:844});await click(results.house.x,results.house.y);assert(await p.locator('#selectedInfo90').isVisible());assert(await p.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));await p.screenshot({path:root+'/work/admin72/screens/info-mobile90.png'});assert.deepEqual(errors,[]);assert.equal(Object.keys(docs).length,0,'read-only inspection wrote data');console.log('PASS building destination, native/painted floors, people, pet owner, palette labels, read-only keyboard, mobile display');}finally{await browser.close();server.close();}})().catch(e=>{console.error(e);server.close();process.exitCode=1});
