const fs=require('fs'),path=require('path'),{pathToFileURL}=require('url');
(async()=>{const root=path.resolve(__dirname,'..'),{buildChapterOne}=await import(pathToFileURL(path.join(root,'src/data/chapterOne.js')).href),{SPECIES}=await import(pathToFileURL(path.join(root,'src/data/species.js')).href),maps=buildChapterOne(),home=structuredClone(maps.hut);Object.assign(home,{id:'adminHouse72',name:'追加した家（共通室内）',npcs:[],items:[],signs:[],objects:[],warps:[{x:7,y:11,to:'@back'}]});maps.adminHouse72=home;const dest=path.join(root,'assets/editor-v72');fs.mkdirSync(dest,{recursive:true});fs.writeFileSync(path.join(dest,'base-maps.json'),JSON.stringify(maps));fs.writeFileSync(path.join(dest,'species.json'),JSON.stringify(Object.keys(SPECIES)));console.log(Object.keys(maps).length,'maps;',Object.keys(SPECIES).length,'species');})().catch(e=>{console.error(e);process.exit(1)});
