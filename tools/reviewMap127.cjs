const fs=require('fs'),assert=require('assert');const {chromium}=require('C:/Users/voraz/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});try{
 const p=await b.newPage({viewport:{width:1000,height:1400}});await p.route('**/src/main.js*',r=>r.fulfill({body:'',contentType:'text/javascript'}));await p.route('**/usage-tracker.js*',r=>r.fulfill({body:'',contentType:'text/javascript'}));await p.goto(process.env.BASE||'http://127.0.0.1:5182/');
 const result=await p.evaluate(async()=>{const a=await import('/src/heroArt127.js'),c=await import('/src/heroCatalog127.mjs');document.body.innerHTML='<canvas width="1000" height="1400"></canvas>';document.body.style='margin:0;background:#dcefe6';const ctx=document.querySelector('canvas').getContext('2d');ctx.imageSmoothingEnabled=false;ctx.fillStyle='#dcefe6';ctx.fillRect(0,0,1000,1400);
 let n=0,count=0;for(const gender of ['boy','girl'])for(const [hair]of c.HAIRSTYLES127[gender]){const y=n*106;ctx.fillStyle='#123447';ctx.font='13px sans-serif';ctx.fillText(gender+' '+hair,0,y+14);
 for(const [k,[color]]of c.OUTFITS127.entries())for(const [d,dir]of ['down','up'].entries()){const look={gender,hairMap119:hair,outfit119:color,hair:location.hash?'#f0f0f0':''};let f;for(let tries=0;tries<100;tries++){f=a.mapHeroFrame127(dir,1,look);if(f)break;await new Promise(r=>setTimeout(r,30));}if(!f)throw Error('Missing '+JSON.stringify(look));ctx.drawImage(f,120+k*176+d*75,y+5,64,96);count++;}n++;}
 return{count};});fs.mkdirSync('artifacts/verify127',{recursive:true});await p.screenshot({path:'artifacts/verify127/map-review.png'});console.log(result);
}finally{await b.close();}})().catch(e=>{console.error(e);process.exit(1)});
