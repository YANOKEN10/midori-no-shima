const fs=require('fs');const {chromium}=require('C:/Users/voraz/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});try{const p=await b.newPage();await p.goto('http://127.0.0.1:5179/');for(let i=0;i<25;i++){const data=await p.evaluate(async i=>{const im=new Image();im.src='/work/item-v13-source/'+i+'.png';await im.decode();const c=document.createElement('canvas');c.width=im.width;c.height=im.height;const ctx=c.getContext('2d',{willReadFrequently:true});ctx.drawImage(im,0,0);const d=ctx.getImageData(0,0,c.width,c.height),a=d.data;let x0=c.width,y0=c.height,x1=0,y1=0;for(let y=0;y<c.height;y++)for(let x=0;x<c.width;x++){if(a[(y*c.width+x)*4+3]<160)continue;x0=Math.min(x0,x);y0=Math.min(y0,y);x1=Math.max(x1,x);y1=Math.max(y1,y);}const out=document.createElement('canvas');out.width=out.height=80;const oc=out.getContext('2d');oc.imageSmoothingEnabled=false;const w=x1-x0+1,h=y1-y0+1,s=Math.min(70/w,70/h),dw=Math.round(w*s),dh=Math.round(h*s);oc.drawImage(c,x0,y0,w,h,Math.floor((80-dw)/2),Math.floor((80-dh)/2),dw,dh);const pix=oc.getImageData(0,0,80,80);for(let k=3;k<pix.data.length;k+=4)pix.data[k]=pix.data[k]>=160?255:0;oc.putImageData(pix,0,0);return out.toDataURL().split(',')[1]},i);fs.writeFileSync('assets/items-v13/'+String(i).padStart(2,'0')+'.png',Buffer.from(data,'base64'));}console.log('25 transparent 80px inventory sprites imported');}finally{await b.close()}})().catch(e=>{console.error(e);process.exit(1)});
