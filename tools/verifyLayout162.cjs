const fs=require('fs'),assert=require('node:assert/strict'),{chromium}=require('C:/Users/voraz/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});try{const pub=await(await fetch('https://midori-no-shima.vercel.app/api/map-editor')).json(),p=await b.newPage();await p.route('**/api/map-editor',r=>r.fulfill({contentType:'application/json',body:JSON.stringify(pub)}));await p.route('**/src/main.js*',r=>r.fulfill({body:''}));await p.goto(process.env.BASE||'http://127.0.0.1:5182',{waitUntil:'domcontentloaded',timeout:60000});const result=await p.evaluate(async()=>{const {MAPS}=await import('/src/data/maps.js'),E=await import('/src/editorModel72.mjs'),M=await import('/src/managedPeople120.mjs'),C=await import('/src/customMaps82.mjs'),A=await import('/src/chapterArt.js'),N=await import('/src/npcArt.js'),P=await import('/src/peopleCatalog119.mjs'),F=await import('/src/peopleArt119.js');const bases=C.extendMaps82(M.addManagedPeople120(await(await fetch('/assets/editor-v72/base-maps.json')).json())),cat=E.catalog(bases,[]),out=[];for(const id of ['leafTown','karatPort','resurePort','belerioPort','towerBattle123']){const base=bases[id],d=E.upgrade79(base,E.initial(base)),m=E.applyEdit(base,d,cat);out.push({id,errors:E.validateEdit(base,d,cat),stable:JSON.stringify(d)===JSON.stringify(E.upgrade79(base,d)),before:base.rows.join('').split('W').length-1,after:MAPS[id].rows.join('').split('W').length-1,grass:MAPS[id].grassBase151,rows:MAPS[id].rows,npcs:MAPS[id].npcs.map(n=>({x:n.x,y:n.y,script:n.script})),variants:MAPS[id].npcs.map(n=>n.variant),props:MAPS[id].props.filter(p=>['eTower','eHouse'].includes(p.art)).map(p=>({art:p.art,x:p.x,y:p.y,w:p.w,h:p.h,door:p.door})),warps:MAPS[id].warps});}window.draw162=()=>{const c=document.createElement('canvas');c.width=1260;c.height=1080;const ctx=c.getContext('2d');ctx.fillStyle='#223c3b';ctx.fillRect(0,0,1260,1080);for(const[id,x,y,scale,crop]of [['leafTown',0,0,1,[16,0,12,22]],['towerBattle123',400,0,.8,[3,4,18,14]],['karatPort',0,720,.65],['belerioPort',650,430,.43]]){const m=MAPS[id],cv=document.createElement('canvas');cv.width=m.rows[0].length*32;cv.height=m.rows.length*32;const a=cv.getContext('2d');A.drawChapterMap(a,m,0,0);for(const n of m.npcs)N.drawNpc(a,n,0,n.x*32,n.y*32-28);ctx.imageSmoothingEnabled=false;const rect=crop||[0,0,m.rows[0].length,m.rows.length];ctx.drawImage(cv,rect[0]*32,rect[1]*32,rect[2]*32,rect[3]*32,x,y,rect[2]*32*scale,rect[3]*32*scale);}return c.toDataURL();};draw162();return {out,guide:P.TOWER_GUIDE162};});for(const m of result.out){assert.deepEqual(m.errors,[],m.id);assert(m.stable);if(m.id==='towerBattle123')assert(m.variants.every(v=>v===result.guide));else{assert(m.grass,m.id);if(m.id==='leafTown')assert.equal(m.after,m.before,m.id+' water');}}for(const m of result.out.filter(m=>m.id.endsWith('Port'))){const shore=m.id==='belerioPort'?23:20,cx=m.id==='belerioPort'?20:14;for(let y=shore;y<m.rows.length;y++)for(let x=0;x<m.rows[0].length;x++)assert.equal(m.rows[y][x],y<shore+3&&x>=cx&&x<cx+2?'d':'W',m.id+' sea/pier '+x+','+y);const n=m.npcs.find(n=>['voyage:board','end:returnIsland'].includes(n.script));assert.equal(m.rows[n.y][n.x],'d');assert.equal(m.rows[shore+1][cx],'d');}for(let i=0;i<32;i++){await p.evaluate(()=>draw162());await p.waitForTimeout(250);}fs.writeFileSync('artifacts/layout162.png',Buffer.from((await p.evaluate(()=>draw162())).split(',')[1],'base64'));console.log(JSON.stringify(result.out.map(m=>({id:m.id,errors:m.errors,stable:m.stable}))));console.log('PASS guides, compact buildings, grass maps, water, stable editable documents');}finally{await b.close()}})().catch(e=>{console.error(e);process.exit(1)});
