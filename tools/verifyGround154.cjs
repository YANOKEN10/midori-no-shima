const fs=require('fs'),assert=require('node:assert/strict'),{chromium}=require('C:/Users/voraz/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});try{const p=await b.newPage();await p.route('**/src/main.js*',r=>r.fulfill({body:''}));await p.goto(process.env.BASE||'http://127.0.0.1:5182',{waitUntil:'domcontentloaded'});const data=await p.evaluate(async()=>{const E=await import('/src/editorModel72.mjs'),{MAPS}=await import('/src/data/maps.js'),A=await import('/src/chapterArt.js'),{addManagedPeople120}=await import('/src/managedPeople120.mjs');const bases=addManagedPeople120(await(await fetch('/assets/editor-v72/base-maps.json')).json()),cat=E.catalog(bases,[]),out=[];for(const id of ['remoteLake','mountainAltar']){const base=bases[id],d=E.upgrade79(base,E.initial(base)),before=E.applyEdit(base,{...E.initial(base),parkGround151:true},cat),m=E.applyEdit(base,d,cat);out.push({id,errors:E.validateEdit(base,d,cat),water:[before,m].map(m=>m.rows.join('').split('W').length-1),grass:[before,m].map(m=>m.rows.join('').split('"').length-1),style:m.grassStyle,tiles:m.editorGround72.filter(t=>t.material!=='grass'),native:d.objects.filter(o=>o.id.startsWith('g:')&&E.objectSource(base,o,cat)?.tile==='"').map(o=>o.template)});}const base=bases.resure,d=E.upgrade79(base,E.initial(base)),rock=d.objects.find(o=>o.y===0&&o.x===4),moved={...d,objects:d.objects.map(o=>o===rock?{...o,x:22,y:10}:o)},m=E.applyEdit(base,moved,cat);out.push({id:'rock',found:!!rock,old:m.rows[0][4],next:m.rows[10][22],errors:E.validateEdit(base,moved,cat),stable:JSON.stringify(moved)===JSON.stringify(E.upgrade79(base,moved))});window.images154=()=>Object.fromEntries(['remoteLake','mountainAltar'].map(id=>{const m=MAPS[id],c=document.createElement('canvas');c.width=m.rows[0].length*32;c.height=m.rows.length*32;A.drawChapterMap(c.getContext('2d'),m,0,0);return [id,c.toDataURL()]}));images154();return out;});for(const r of data){assert.deepEqual(r.errors,[],r.id);if(r.id==='rock'){assert(r.found);assert.notEqual(r.old,'R');assert.equal(r.next,'R');assert(r.stable);}else{assert.equal(r.water[0],r.water[1]);assert.equal(r.grass[0],r.grass[1]);assert.equal(r.style,'mountain');assert.deepEqual(r.tiles,[]);assert(r.native.every(t=>t==='legacy73-grass-v25-mountain'));}}await p.waitForTimeout(2000);for(const[id,url]of Object.entries(await p.evaluate(()=>images154())))fs.writeFileSync('artifacts/'+id+'154.png',Buffer.from(url.split(',')[1],'base64'));console.log('PASS lake/altar grass floors, preserved water and grass, mountain grass asset; border rock move and stable migration');}finally{await b.close();}})().catch(e=>{console.error(e);process.exit(1)});


