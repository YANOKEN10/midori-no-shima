const {chromium}=require('C:/Users/voraz/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),fs=require('fs');
(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});try{const p=await b.newPage();await p.goto('http://127.0.0.1:5179/');const outputs=await p.evaluate(async()=>{const img=async(name)=>{const im=new Image();im.src='/assets/world-v19/'+name+'-source.png';await im.decode();return im;};const canvas=(w,h)=>{const c=document.createElement('canvas');c.width=w;c.height=h;c.getContext('2d').imageSmoothingEnabled=false;return c;};const out={};const tree=await img('tree'),t=canvas(tree.width,tree.height),tc=t.getContext('2d');tc.drawImage(tree,0,0);const d=tc.getImageData(0,0,t.width,t.height);let x0=t.width,y0=t.height,x1=0,y1=0;for(let y=0;y<t.height;y++)for(let x=0;x<t.width;x++){let i=(y*t.width+x)*4;const r=d.data[i],g=d.data[i+1],b=d.data[i+2];if(Math.max(r,g,b)-Math.min(r,g,b)<35){d.data[i+3]=0;}else{x0=Math.min(x0,x);y0=Math.min(y0,y);x1=Math.max(x1,x);y1=Math.max(y1,y);}}tc.putImageData(d,0,0);let c=canvas(64,80);c.getContext('2d').drawImage(t,x0,y0,x1-x0+1,y1-y0+1,0,0,64,80);out.tree=c.toDataURL();c=canvas(128,128);c.getContext('2d').drawImage(await img('forest'),0,0,128,128);out.forest=c.toDataURL();const heroes=await img('trainers');c=canvas(160,88);for(let k=0;k<2;k++){const part=canvas(887,887),ctx=part.getContext('2d');ctx.drawImage(heroes,k*887,0,887,887,0,0,887,887);const a=ctx.getImageData(0,0,887,887).data;let l=887,r=0,u=887,v=0;for(let y=0;y<887;y++)for(let x=0;x<887;x++)if(a[(y*887+x)*4+3]>127){l=Math.min(l,x);r=Math.max(r,x);u=Math.min(u,y);v=Math.max(v,y);}c.getContext('2d').drawImage(part,l,u,r-l+1,v-u+1,k*80,0,80,88);}out.trainers=c.toDataURL();const bg=await img('battle');c=canvas(320,288);c.getContext('2d').drawImage(bg,0,0,1322,874,0,0,320,208);c.getContext('2d').drawImage(bg,0,874,1322,316,0,208,320,80);out.battle=c.toDataURL();return out;});for(const[n,data]of Object.entries(outputs))fs.writeFileSync('assets/world-v19/'+n+'.png',Buffer.from(data.split(',')[1],'base64'));}finally{await b.close();}})();
