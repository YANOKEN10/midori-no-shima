const fs=require('fs'),assert=require('node:assert/strict'),{chromium}=require('C:/Users/voraz/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});try{const p=await b.newPage({viewport:{width:1500,height:1100}});let saved;await p.addInitScript(()=>localStorage.setItem('vmon:token','test'));await p.route('**/api/map-editor',r=>{const d=r.request().method()==='POST'?r.request().postDataJSON():{};if(d.action==='save')saved=d.edit;return r.fulfill({contentType:'application/json',body:JSON.stringify(d.action==='session'?{name:'検証',definitions:{}}:d.action==='read'?{draft:null,draftRevision:0,publicRevision:0}:d.action==='save'?{ok:true,revision:1}:{maps:{},definitions:{}})});});await p.goto((process.env.BASE||'http://127.0.0.1:5182')+'/map-editor/',{waitUntil:'domcontentloaded',timeout:60000});await p.waitForSelector('#mapSelect:not([disabled])');for(const id of ['natureforest','mossSanctuary']){await p.selectOption('#mapSelect',id);await p.waitForTimeout(1500);assert((await p.locator('#validation').innerText()).includes('通過'),id+': '+await p.locator('#validation').innerText());await p.click('#save');await p.waitForTimeout(300);assert(saved.defaultBorders143);await p.locator('#mapCanvas').screenshot({path:'artifacts/'+id+'143.png'});}console.log('PASS untouched forest and sanctuary UI validation and save');}finally{await b.close();}})().catch(e=>{console.error(e);process.exit(1)});