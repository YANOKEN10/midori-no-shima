const fs=require('fs'),http=require('http'),path=require('path'),assert=require('assert/strict');
const root=path.resolve(__dirname,'..'),L=require(root+'/api/_lib'),F=require(root+'/api/_friendStore');
const admin={id:'ヤノケン',created:1789724902905,display:'ヤノケン'},ordinary={id:'tester',created:1};L.setPw(admin,'test-only-password');let docs={};L.readUser=async id=>id===admin.id?admin:id==='tester'?ordinary:null;L.findUser=async name=>name===admin.id?admin:null;L.configured=()=>true;F.read=async keys=>Object.fromEntries(keys.filter(k=>docs[k]).map(k=>[k,structuredClone(docs[k])]));F.commit=async(expected,writes)=>{if(Object.entries(expected).some(([k,v])=>(docs[k]?.version||0)!==v))return false;for(const[k,v]of Object.entries(writes))docs[k]={version:(docs[k]?.version||0)+1,data:structuredClone(v)};return true;};
const handler=require(root+'/api/map-editor'),auth=require(root+'/api/auth');
const mime={'.js':'text/javascript','.mjs':'text/javascript','.json':'application/json','.css':'text/css','.html':'text/html','.png':'image/png','.webp':'image/webp','.mp3':'audio/mpeg'};
const server=http.createServer(async(req,res)=>{res.status=c=>{res.statusCode=c;return res};res.json=v=>{res.setHeader('Content-Type','application/json');res.end(JSON.stringify(v))};const url=new URL(req.url,'http://localhost');if(url.pathname.startsWith('/api/')){let body='';for await(const b of req)body+=b;try{req.body=JSON.parse(body||'{}');}catch{req.body={};}if(url.pathname==='/api/map-editor')return handler(req,res);if(url.pathname==='/api/auth')return auth(req,res);return res.status(200).json({});}let file=path.join(root,decodeURIComponent(url.pathname));if(!file.startsWith(path.normalize(root)))return res.status(403).end();try{if(fs.statSync(file).isDirectory())file=path.join(file,'index.html');res.setHeader('Content-Type',mime[path.extname(file)]||'application/octet-stream');fs.createReadStream(file).pipe(res);}catch{res.status(404).end();}});
(async()=>{await new Promise(r=>server.listen(5183,'127.0.0.1',r));const m=await import(require('url').pathToFileURL(root+'/src/editorModel72.mjs').href),maps=require(root+'/assets/editor-v72/base-maps.json'),cat=m.catalog(maps,require(root+'/assets/editor-v72/species.json')),base=maps.rods;for(const b of Object.values(maps))assert.deepEqual(m.validateEdit(b,m.initial(b),cat),[],b.id);let spot;outer:for(let y=4;y<base.rows.length-3;y++)for(let x=3;x<base.rows[0].length-3;x++){const d=m.initial(base);d.objects.push({id:'a:sign',type:'prop',template:'sign82',x,y,signText82:'こんにちは\nガオンの町へようこそ'});if(!m.validateEdit(base,d,cat).length&&m.pass(base,x,y+1)){spot={x,y};break outer;}}assert(spot);
 const d=m.initial(base),old=d.objects.find(o=>m.isSign82(m.objectSource(base,o,cat)));assert(old);old.signText82='既存の看板を編集';let changed=m.applyEdit(base,d,cat);assert(changed.signs.some(s=>s.text[0]===old.signText82));old.stored79=true;changed=m.applyEdit(base,d,cat);assert(!changed.signs.some(s=>s.text[0]==='既存の看板を編集'));old.signText82='a'.repeat(501);assert(m.validateEdit(base,d,cat).length);const legacy=m.initial(base);delete legacy.signs82;legacy.objects=legacy.objects.filter(o=>o.template!=='sign');assert(m.upgrade79(base,legacy).objects.some(o=>o.template==='sign'));
 const {chromium}=require('C:/Users/voraz/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),browser=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});try{const p=await browser.newPage({viewport:{width:1440,height:1000}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:5183/map-editor/');await p.locator('#password').fill('test-only-password');await p.locator('#loginForm button').click();await p.waitForFunction(()=>!document.querySelector('.layout').inert&&!document.getElementById('app').hidden);await p.locator('#mapSelect').selectOption('rods');await p.waitForFunction(()=>!document.querySelector('.layout').inert);await p.locator('[data-category="signs"]').click();await p.locator('[data-key="sign82"]').click();await p.evaluate(({x,y})=>{const c=document.getElementById('mapCanvas'),r=c.getBoundingClientRect();c.setPointerCapture=()=>{};c.dispatchEvent(new PointerEvent('pointerdown',{pointerId:1,clientX:r.left+(x+.5)*r.width/c.width*32,clientY:r.top+(y+.5)*r.height/c.height*32,bubbles:true}));},spot);await p.getByLabel('看板の文章',{exact:true}).fill('こんにちは\nガオンの町へようこそ');await p.getByLabel('看板の文章',{exact:true}).press('Tab');assert(!(await p.locator('#validation').textContent()).includes('重な'));await p.locator('#save').click();await p.waitForFunction(()=>document.getElementById('dirty').textContent==='下書き保存済み');assert(docs['maps72:draft:rods'].data.edit.objects.some(o=>o.signText82==='こんにちは\nガオンの町へようこそ'));await p.locator('#publish').click();await p.locator('#confirmPublish').click();await p.waitForTimeout(500);await p.reload();await p.waitForFunction(()=>!document.querySelector('.layout').inert&&!document.getElementById('app').hidden);
 const game=await browser.newPage();game.on('pageerror',e=>errors.push(e.message));await game.goto('http://127.0.0.1:5183/?v4test=rods');await game.waitForFunction(()=>window.VM);const got=await game.evaluate(async({x,y})=>{const {ui}=await import('/src/ui.js');VM.world.enter('rods',x,y+1,'up');let lines;const say=ui.say;ui.say=async a=>{lines=a;};try{VM.world.interact();await new Promise(r=>setTimeout(r,100));}finally{ui.say=say;}return lines;},spot);assert.deepEqual(got,['こんにちは','ガオンの町へようこそ']);assert.deepEqual(errors,[]);console.log('PASS v82: 88 maps; legacy sign migration; existing text edit/storage; length validation; UI placement/text/save/publish/reload; actual game A reads multiline text',spot);
 }finally{await browser.close();server.close();}})().catch(e=>{console.error(e);server.close();process.exitCode=1});
