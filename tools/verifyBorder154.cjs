const fs=require('fs'),assert=require('node:assert/strict'),{chromium}=require('C:/Users/voraz/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');(async()=>{const published=await(await fetch('https://midori-no-shima.vercel.app/api/map-editor')).json();const b=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});try{const p=await b.newPage();await p.route('**/src/main.js*',r=>r.fulfill({body:''}));await p.route('**/api/map-editor',r=>r.fulfill({contentType:'application/json',body:JSON.stringify(published)}));await p.goto(process.env.BASE||'http://127.0.0.1:5182',{waitUntil:'domcontentloaded',timeout:60000});const result=await p.evaluate(async published=>{const E=await import('/src/editorModel72.mjs'),{MAPS}=await import('/src/data/maps.js'),{addManagedPeople120}=await import('/src/managedPeople120.mjs'),{extendMaps82}=await import('/src/customMaps82.mjs'),{connectionFrames113}=await import('/map-editor/connectionFrames113.js'),A=await import('/src/chapterArt.js'),N=await import('/src/npcArt.js');const bases=extendMaps82(addManagedPeople120(await(await fetch('/assets/editor-v72/base-maps.json')).json()),published.definitions),cat=E.catalog(bases,await(await fetch('/assets/editor-v72/species.json')).json()),base=bases.resure,d=E.upgrade79(base,published.maps.resure||E.initial(base)),m=E.applyEdit(base,d,cat),runtime=MAPS.resure;window.entrances154=()=>{const c=document.createElement('canvas');c.width=1088;c.height=352;const ctx=c.getContext('2d');A.drawChapterMap(ctx,runtime,0,0);for(const n of runtime.npcs)if(n.y<11)N.drawNpc(ctx,n,0,n.x*32,n.y*32-28);for(const f of connectionFrames113(runtime)){ctx.strokeStyle=f.to==='route11'?'#fff36b':'#5fe1fa';ctx.lineWidth=2;ctx.strokeRect(f.x*32+1,f.y*32+1,f.w*32-2,f.h*32-2);}return c.toDataURL();};entrances154();return {errors:E.validateEdit(base,d,cat),stable:JSON.stringify(d)===JSON.stringify(E.upgrade79(base,d)),editorFrames:connectionFrames113(m).filter(f=>['route11','gaonPark'].includes(f.to)),runtimeFrames:connectionFrames113(runtime).filter(f=>['route11','gaonPark'].includes(f.to)),closed:[runtime.rows[0][29],runtime.rows[0][30]],road:MAPS.route11.warps.filter(w=>w.to==='resure').map(w=>({tx:w.tx,ty:w.ty})),park:MAPS.gaonPark.warps.filter(w=>w.to==='resure').map(w=>({tx:w.tx,ty:w.ty})),guards:runtime.npcs.filter(n=>n.script==='frontier:guard').map(n=>[n.x,n.y]),requirements:runtime.warps.filter(w=>w.to==='route11').map(w=>w.requires)};},published);assert.deepEqual(result.errors,[]);assert(result.stable);for(const frames of [result.editorFrames,result.runtimeFrames]){assert.deepEqual(frames.find(f=>f.to==='route11'),{x:9,y:0,w:2,h:1,to:'route11'});assert.deepEqual(frames.find(f=>f.to==='gaonPark'),{x:29,y:3,w:2,h:1,to:'gaonPark'});}assert(result.closed.every(ch=>ch!=='R'));assert.deepEqual(result.road,[{tx:9,ty:1},{tx:10,ty:1}]);assert.deepEqual(result.park,[{tx:29,ty:4},{tx:30,ty:4}]);assert.deepEqual(result.guards,[[8,1],[11,1]]);assert(result.requirements.every(r=>r==='frontier:five'));await p.waitForTimeout(2000);fs.writeFileSync('artifacts/resure-entrances154.png',Buffer.from((await p.evaluate(()=>entrances154())).split(',')[1],'base64'));console.log('PASS published editor layout + runtime: separated two-cell road/park exits, gate moved to border, both return destinations, guards, progression lock and stable migration');}finally{await b.close();}})().catch(e=>{console.error(e);process.exit(1)});
