const fs=require('fs'),assert=require('node:assert/strict'),{chromium}=require('C:/Users/voraz/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});try{const published=await(await fetch('https://midori-no-shima.vercel.app/api/map-editor')).json();const p=await b.newPage();await p.route('**/api/map-editor',r=>r.fulfill({contentType:'application/json',body:JSON.stringify(published)}));await p.route('**/src/main.js*',r=>r.fulfill({body:''}));await p.goto(process.env.BASE||'http://127.0.0.1:5182',{waitUntil:'domcontentloaded',timeout:60000});const result=await p.evaluate(async()=>{const published=await(await fetch('/api/map-editor')).json();const E=await import('/src/editorModel72.mjs'),{MAPS}=await import('/src/data/maps.js'),A=await import('/src/chapterArt.js'),N=await import('/src/npcArt.js'),{talkCells135}=await import('/src/counterTalk135.mjs'),{addManagedPeople120}=await import('/src/managedPeople120.mjs');const bases=addManagedPeople120(await(await fetch('/assets/editor-v72/base-maps.json')).json()),cat=E.catalog(bases,[]),out=[];for(const base of Object.values(bases).filter(m=>m.fashionTown)){const d=E.upgrade79(base,E.initial(base)),m=E.applyEdit(base,JSON.parse(JSON.stringify(d)),cat);const pub=published;out.push({runtimeErrors:pub.maps?.[base.id]&&!MAPS[base.id].fashion159?E.validateEdit(MAPS[base.id],pub.maps[base.id],E.catalog(MAPS,[])):[],publicErrors:pub.maps?.[base.id]?E.validateEdit(base,pub.maps[base.id],cat):[],id:base.id,errors:E.validateEdit(base,d,cat),stable:JSON.stringify(d)===JSON.stringify(E.upgrade79(base,d)),runtime:MAPS[base.id].fashion159,clerk:MAPS[base.id].npcs[0].variant,clothes:MAPS[base.id].npcs[0].clothes,talk:talkCells135(m,5,5,0,-1),names:m.npcs.map(n=>n.displayName||n.name),furniture:m.room.furniture.map(f=>f.slice(0,5))});}window.fashionImage159=()=>{const m=MAPS['fashion-village'],c=document.createElement('canvas');c.width=512;c.height=448;const ctx=c.getContext('2d');A.drawChapterMap(ctx,m,0,0);for(const n of m.npcs)N.drawNpc(ctx,n,0,n.x*32,n.y*32-28);return c.toDataURL();};fashionImage159();return out;});console.log(JSON.stringify(result.map(r=>({id:r.id,runtime:r.runtime,publicErrors:r.publicErrors,runtimeErrors:r.runtimeErrors}))));assert.equal(result.length,10);const names=[];for(const r of result){assert.deepEqual(r.errors,[],r.id);assert.deepEqual(r.publicErrors,[],r.id);assert(r.stable);assert(r.runtime,r.id);assert(r.clothes);assert.notEqual(r.clerk,5);assert(r.talk.some(p=>p[0]===5&&p[1]===3));names.push(...r.names);}assert.equal(new Set(names).size,names.length,'unique names');for(let i=1;i<result.length;i++)assert.deepEqual(result[i].furniture,result[0].furniture);for(let i=0;i<(process.env.BASE?100:24);i++){await p.evaluate(()=>fashionImage159());await p.waitForTimeout(250);}fs.writeFileSync('artifacts/fashion159.png',Buffer.from((await p.evaluate(()=>fashionImage159())).split(',')[1],'base64'));console.log('PASS all 10 shops: matching furniture, new people, unique names, clothing service, counter interaction, migration, entrances');}finally{await b.close()}})().catch(e=>{console.error(e);process.exit(1)});
