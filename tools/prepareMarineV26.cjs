const {chromium}=require('C:/Users/voraz/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),fs=require('fs');
(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});try{const p=await b.newPage();await p.goto('http://127.0.0.1:5179/gaon-zukan/');const out=await p.evaluate(async()=>{const out=[];for(const [source,names] of [['harbor',['marineHouse','marineHall','marineShop','marinePier']],['nature',['ancientTree','shoreRock','ancientAltar','marineChest']]]){const im=new Image();im.src='/assets/marine-v26/'+source+'-source.png';await im.decode();for(let i=0;i<4;i++){const cv=document.createElement('canvas');cv.width=im.width/2;cv.height=im.height/2;const c=cv.getContext('2d');c.drawImage(im,i%2*cv.width,Math.floor(i/2)*cv.height,cv.width,cv.height,0,0,cv.width,cv.height);const a=c.getImageData(0,0,cv.width,cv.height).data;let l=cv.width,t=cv.height,r=0,b=0;for(let y=0;y<cv.height;y++)for(let x=0;x<cv.width;x++)if(a[(y*cv.width+x)*4+3]>100){l=Math.min(l,x);r=Math.max(r,x);t=Math.min(t,y);b=Math.max(b,y);}const d=document.createElement('canvas');const name=names[i];[d.width,d.height]=name==='ancientTree'?[96,128]:name==='marinePier'?[96,64]:name==='marineChest'?[32,32]:[160,160];const dc=d.getContext('2d');dc.imageSmoothingEnabled=false;dc.drawImage(cv,l,t,r-l+1,b-t+1,1,1,d.width-2,d.height-2);out.push({name,data:d.toDataURL().split(',')[1]});}}
for(const name of ['sea','lake','reeds']){const im=new Image();im.src='/assets/marine-v26/'+name+'-source.png';await im.decode();const cv=document.createElement('canvas');cv.width=cv.height=name==='reeds'?32:128;const c=cv.getContext('2d');c.imageSmoothingEnabled=false;c.drawImage(im,0,0,cv.width,cv.height);out.push({name,data:cv.toDataURL().split(',')[1]});}return out;});for(const o of out)fs.writeFileSync('assets/marine-v26/'+o.name+'.png',Buffer.from(o.data,'base64'));}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1});
