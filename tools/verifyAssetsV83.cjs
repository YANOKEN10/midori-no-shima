const fs=require('fs'),http=require('http'),path=require('path'),assert=require('assert/strict');
const root=path.resolve(__dirname,'..'),L=require(root+'/api/_lib'),F=require(root+'/api/_friendStore');
const admin={id:'ヤノケン',created:1789724902905,display:'ヤノケン'},ordinary={id:'tester',created:1};L.setPw(admin,'test-only-password');let docs={};L.readUser=async id=>id===admin.id?admin:id==='tester'?ordinary:null;L.findUser=async name=>name===admin.id?admin:null;L.configured=()=>true;F.read=async keys=>Object.fromEntries(keys.filter(k=>docs[k]).map(k=>[k,structuredClone(docs[k])]));F.commit=async(expected,writes)=>{if(Object.entries(expected).some(([k,v])=>(docs[k]?.version||0)!==v))return false;for(const[k,v]of Object.entries(writes))docs[k]={version:(docs[k]?.version||0)+1,data:structuredClone(v)};return true;};
const handler=require(root+'/api/map-editor'),auth=require(root+'/api/auth');
const mime={'.js':'text/javascript','.mjs':'text/javascript','.json':'application/json','.css':'text/css','.html':'text/html','.png':'image/png','.webp':'image/webp','.mp3':'audio/mpeg'};
const server=http.createServer(async(req,res)=>{res.status=c=>{res.statusCode=c;return res};res.json=v=>{res.setHeader('Content-Type','application/json');res.end(JSON.stringify(v))};const url=new URL(req.url,'http://localhost');if(url.pathname.startsWith('/api/')){let body='';for await(const b of req)body+=b;try{req.body=JSON.parse(body||'{}');}catch{req.body={};}if(url.pathname==='/api/map-editor')return handler(req,res);if(url.pathname==='/api/auth')return auth(req,res);return res.status(200).json({});}let file=path.join(root,decodeURIComponent(url.pathname));if(!file.startsWith(path.normalize(root)))return res.status(403).end();try{if(fs.statSync(file).isDirectory())file=path.join(file,'index.html');res.setHeader('Content-Type',mime[path.extname(file)]||'application/octet-stream');fs.createReadStream(file).pipe(res);}catch{res.status(404).end();}});
(async()=>{await new Promise(r=>server.listen(5183,'127.0.0.1',r));const {chromium}=require('C:/Users/voraz/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),browser=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});try{const p=await browser.newPage({viewport:{width:1440,height:1000}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:5183/map-editor/');await p.locator('#password').fill('test-only-password');await p.locator('#loginForm button').click();await p.waitForFunction(()=>!document.querySelector('.layout').inert&&!document.getElementById('app').hidden);
const result=await p.evaluate(async()=>{const M=await import('/src/editorModel72.mjs'),{extendMaps82}=await import('/src/customMaps82.mjs'),{BUILDINGS83}=await import('/src/buildingRooms83.mjs'),{canTraverse75}=await import('/src/elevation75.mjs');const base=extendMaps82(await fetch('/assets/editor-v72/base-maps.json').then(r=>r.json())),cat=M.catalog(base,[]),check=(a,b)=>{if(!a)throw Error(b);};const flat={id:'test83',name:'test',kind:'out',rows:Array(32).fill(','.repeat(32)),spawn:{x:16,y:26},props:[],npcs:[],warps:[],signs:[],items:[],objects:[]};for(const art of Object.keys(BUILDINGS83)){const d=M.initial(flat);d.objects.push({id:'a:building',type:'prop',template:art,x:8,y:3});const err=M.validateEdit(flat,d,cat);check(!err.length,art+err.join());const m=M.applyEdit(flat,d,cat),wp=m.warps[0],src=cat.props.find(p=>p.key===art),room=base[wp.to];check(M.pass(m,wp.x,wp.y)&&M.pass(m,wp.back.x,wp.back.y),'door '+art);check(wp.x===8+Math.floor(src.w/2),'center '+art);check(M.pass(room,wp.tx,wp.ty)&&room.warps.some(w=>w.to==='@back'),'room '+art);}
for(const art of ['tree','fir','garden72-conifer','garden72-broadleaf']){const d=M.initial(flat);d.objects.push({id:'a:tree',type:'prop',template:art,x:8,y:8});const m=M.applyEdit(flat,d,cat);for(let y=7;y<=10;y++)for(let x=8;x<=9;x++)check(M.pass(m,x,y)===(y===7),'tree '+art+' '+x+','+y);}
const d=M.initial(flat);d.objects.push({id:'a:stairs',type:'prop',template:'legacy73-stairs',x:5,y:5});check(M.pass(M.applyEdit(flat,d,cat),5,5),'stairs blocked');const elevated={elevationIndex75:{'5,4':1},climbs75:[{x:5,y:5,kind:'stairs'}]};check(canTraverse75(elevated,5,5,5,4)&&canTraverse75(elevated,5,4,5,5),'stairs directions');check(!canTraverse75(elevated,4,4,5,4),'side cliff');
const {drawWater83}=await import('/src/waterSurface83.js'),cv=document.createElement('canvas');cv.width=cv.height=64;const c=cv.getContext('2d'),map={rows:['WW','WW'],props:[]};drawWater83(c,map,0,0,0);const before=cv.toDataURL();c.clearRect(0,0,64,64);drawWater83(c,map,0,0,900);check(before!==cv.toDataURL(),'water static');return {buildings:Object.keys(BUILDINGS83).length,trees:4,stairs:true,water:true};});
await p.locator('[data-category=buildings83]').click();await p.locator('#search').fill('石造り');await p.waitForTimeout(1500);await p.screenshot({path:'work/admin72/screens/house83.png'});
await p.locator('#mapSelect').selectOption('building83-marineHouse');await p.waitForFunction(()=>!document.querySelector('.layout').inert);await p.waitForTimeout(1500);await p.screenshot({path:'work/admin72/screens/room83.png'});
const jungle='mossSanctuary';if(jungle){await p.locator('#mapSelect').selectOption(jungle);await p.waitForFunction(()=>!document.querySelector('.layout').inert);await p.waitForTimeout(1500);await p.screenshot({path:'work/admin72/screens/jungle83.png'});}
await p.locator('#mapSelect').selectOption('building83-marineHouse');await p.waitForFunction(()=>!document.querySelector('.layout').inert);const cv=p.locator('#map'),canvasId=await p.locator('main canvas').first().getAttribute('id');const canvas=p.locator('#'+canvasId),rect=await canvas.boundingBox();const before=await canvas.evaluate(c=>c.getBoundingClientRect().width);await p.mouse.move(rect.x+100,rect.y+100);await p.mouse.wheel(0,-240);await p.waitForTimeout(300);assert((await canvas.evaluate(c=>c.getBoundingClientRect().width))>before,'main wheel zoom');
await p.locator('#newMap82').click();await p.locator('#newMapName82').fill('連続配置の検証');await p.locator('#newMapForm82 button[type=submit]').click();await p.waitForFunction(()=>!document.querySelector('.layout').inert&&!document.getElementById('newMapDialog82').open);await p.locator('[data-category=signs]').click();await p.locator('[data-key=sign82]').click();const clickTile=async(x,y)=>{await p.locator('main canvas').first().evaluate((c,{x,y})=>{const r=c.getBoundingClientRect(),sx=r.width/c.width,sy=r.height/c.height;c.setPointerCapture=()=>{};for(const type of ['pointerdown','pointerup'])c.dispatchEvent(new PointerEvent(type,{bubbles:true,pointerId:1,pointerType:'mouse',button:0,clientX:r.x+(x*32+16)*sx,clientY:r.y+(y*32+16)*sy}));},{x,y});};await clickTile(3,3);await clickTile(6,3);await p.locator('#save').click();await p.waitForFunction(()=>document.getElementById('dirty').textContent==='下書き保存済み');const created=await p.locator('#mapSelect').inputValue();assert.equal(docs['maps72:draft:'+created].data.edit.objects.filter(o=>o.template==='sign82').length,2,'continuous placement');await p.locator('[data-category=links]').click();await p.locator('[data-key=entrance]').click();await clickTile(9,9);assert(await p.locator('#connectionDialog83').isVisible());await p.locator('#connectionTarget83').selectOption('building83-marineHouse');await p.locator('#connectionCanvas83').evaluate(c=>{const r=c.getBoundingClientRect();c.dispatchEvent(new MouseEvent('click',{bubbles:true,clientX:r.x+r.width*7.5/16,clientY:r.y+r.height*10.5/14}));});await p.locator('#applyConnection83').click();assert(!(await p.locator('#connectionDialog83').isVisible()),await p.locator('#connectionError83').textContent());await p.locator('#save').click();await p.waitForFunction(()=>document.getElementById('dirty').textContent==='下書き保存済み');assert.equal(docs['maps72:draft:'+created].data.edit.links.length,1);const game=await browser.newPage();game.on('pageerror',e=>errors.push(e.message));await game.goto('http://127.0.0.1:5183/?v4test=rods');await game.waitForFunction(()=>window.VM);await game.evaluate(async()=>{const {MAPS}=await import('/src/data/maps.js?v=20260913-fashion-v50');for(const id of Object.keys(MAPS).filter(id=>id.startsWith('building83-'))){VM.world.enter('rods',16,20,'up');await VM.world.doWarp({to:id,tx:7,ty:11,back:{map:'rods',x:16,y:20}});if(VM.world.mapId!==id)throw Error('enter '+id);await VM.world.doWarp(VM.world.map.warps.find(w=>w.to==='@back'));if(VM.world.mapId!=='rods')throw Error('return '+id);}});assert.deepEqual(errors,[]);console.log('PASS v83',JSON.stringify(result));}finally{await browser.close();server.close();}})().catch(e=>{console.error(e);server.close();process.exitCode=1});
