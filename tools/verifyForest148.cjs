const fs=require('fs'),assert=require('node:assert/strict'),{chromium}=require('C:/Users/voraz/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});try{const p=await b.newPage();await p.route('**/src/main.js*',r=>r.fulfill({body:'',contentType:'text/javascript'}));await p.route('**/usage-tracker.js*',r=>r.fulfill({body:''}));await p.goto(process.env.BASE||'http://127.0.0.1:5182',{waitUntil:'domcontentloaded',timeout:60000});const report=await p.evaluate(async()=>{const E=await import('/src/editorModel72.mjs'),{MAPS}=await import('/src/data/maps.js'),A=await import('/src/chapterArt.js'),{addManagedPeople120}=await import('/src/managedPeople120.mjs');const base=addManagedPeople120(await (await fetch('/assets/editor-v72/base-maps.json')).json()),cat=E.catalog(base,[]),report=[];for(const id of ['natureforest','mossSanctuary']){const d=E.upgrade79(base[id],E.initial(base[id])),m=E.applyEdit(base[id],d,cat);for(const [kind,map]of [['editor',m],['runtime',MAPS[id]]])report.push({id,kind,grass:map.rows.join('').split('"').length-1,grassProps:map.editorObjects78?.filter(o=>o.group==='grass'||o.tile==='"').length,trees:map.editorObjects78?.filter(o=>/tree|fir|conifer|broadleaf|palm/i.test(o.art)).map(o=>o.art),errors:kind==='editor'?E.validateEdit(base[id],d,cat):[]});}window.renderForest148=()=>{const out={};for(const id of ['natureforest','mossSanctuary']){const m=MAPS[id],c=document.createElement('canvas');c.width=m.rows[0].length*32;c.height=m.rows.length*32;A.drawChapterMap(c.getContext('2d'),m,0,0);out[id]=c.toDataURL();}return out;};renderForest148();return report;});for(const r of report){assert.equal(r.grass,0,JSON.stringify(r));assert.equal(r.grassProps,0);assert.deepEqual(r.errors,[]);if(r.id==='mossSanctuary'){assert(r.trees.length>0);assert(r.trees.every(a=>a==='mountain-conifer131'),JSON.stringify(r));}}await p.waitForTimeout(2500);for(const[id,url]of Object.entries(await p.evaluate(()=>renderForest148())))fs.writeFileSync('artifacts/'+id+'148.png',Buffer.from(url.split(',')[1],'base64'));console.log('PASS editor + runtime: both forests have no tall grass; sanctuary trees all mountain conifers; entrances valid');}finally{await b.close();}})().catch(e=>{console.error(e);process.exit(1)});
