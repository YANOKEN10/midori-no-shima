const {chromium}=require('C:/Users/voraz/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),fs=require('fs');
(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});try{const p=await b.newPage();await p.goto('http://127.0.0.1:5179/gaon-zukan/');for(const[size,name,mask]of [[180,'apple-touch-v2',false],[192,'icon-192-v2',false],[512,'icon-512-v2',false],[512,'maskable-512-v2',true],[32,'favicon-v2',false]]){const data=await p.evaluate(async({size,mask})=>{const im=new Image();im.src='/gaon-zukan/icons/zukan-source-v2.png';await im.decode();const c=document.createElement('canvas');c.width=c.height=size;const ctx=c.getContext('2d');ctx.fillStyle='#183e34';ctx.fillRect(0,0,size,size);ctx.imageSmoothingEnabled=true;ctx.imageSmoothingQuality='high';const pad=mask?Math.round(size*.15):0;ctx.drawImage(im,pad,pad,size-pad*2,size-pad*2);return c.toDataURL('image/png').split(',')[1]},{size,mask});fs.writeFileSync('gaon-zukan/icons/'+name+'.png',Buffer.from(data,'base64'));}console.log('Five icon sizes created');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1});
