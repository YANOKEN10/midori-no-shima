const{chromium}=require('C:/Users/voraz/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),fs=require('fs');
(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});try{const p=await b.newPage();await p.goto('http://127.0.0.1:5179/');const output=await p.evaluate(async()=>{const result=[];for(const [file,names,sizes] of [['power',['flowerHouse','powerStation','solarPanels','generator'],[[160,160],[288,224],[160,96],[128,96]]],['machines',['controlBank','transformers','pipePump','turbine'],[[128,96],[128,96],[128,96],[128,96]]],['flowers',['flowersPink','flowersGold','ferry','flowerTree'],[[64,64],[64,64],[288,160],[64,96]]]]){const im=new Image();im.src='/assets/power-v27/'+file+'-source.png';await im.decode();const src=document.createElement('canvas');src.width=im.width;src.height=im.height;const sc=src.getContext('2d');sc.drawImage(im,0,0);const d=sc.getImageData(0,0,im.width,im.height).data;for(let n=0;n<4;n++){const cx=file==='flowers'&&n===3?Math.ceil(im.width*.55):file==='machines'&&n%2?Math.ceil(im.width*.53):(n%2)*im.width/2,cy=file==='machines'&&n>=2?Math.floor(im.height*.45):Math.floor(n/2)*im.height/2,ey=file==='machines'?(n<2?Math.floor(im.height*.45):im.height):cy+im.height/2,ex=file==='flowers'&&n===2||file==='machines'&&n%2===0?Math.floor(im.width*.53):n%2?im.width:im.width/2;let x0=ex,y0=ey,x1=cx,y1=cy;for(let y=cy;y<ey;y++)for(let x=cx;x<ex;x++)if(d[(y*im.width+x)*4+3]>30){x0=Math.min(x0,x);y0=Math.min(y0,y);x1=Math.max(x1,x);y1=Math.max(y1,y);}const c=document.createElement('canvas');[c.width,c.height]=sizes[n];const ctx=c.getContext('2d');ctx.imageSmoothingEnabled=false;ctx.drawImage(im,x0,y0,x1-x0+1,y1-y0+1,0,0,c.width,c.height);result.push({name:names[n],rect:[x0,y0,x1-x0+1,y1-y0+1],png:c.toDataURL().split(',')[1]});}}return result;});for(const o of output){fs.writeFileSync('assets/power-v27/'+o.name+'.png',Buffer.from(o.png,'base64'));console.log(o.name,o.rect);}}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1});
