const{chromium}=require('C:/Users/voraz/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),fs=require('fs');
(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});try{const p=await b.newPage();await p.goto('http://127.0.0.1:5179/');const out=await p.evaluate(async()=>{const im=new Image();im.src='/assets/grass-v25/source.png';await im.decode();return ['mountain','rural'].map((name,i)=>{const c=document.createElement('canvas');c.width=im.width/2;c.height=im.height;const ctx=c.getContext('2d');ctx.drawImage(im,i*c.width,0,c.width,c.height,0,0,c.width,c.height);const d=ctx.getImageData(0,0,c.width,c.height);let l=c.width,t=c.height,r=0,b=0;for(let y=0;y<c.height;y++)for(let x=0;x<c.width;x++)if(d.data[(y*c.width+x)*4+3]>64){l=Math.min(l,x);t=Math.min(t,y);r=Math.max(r,x);b=Math.max(b,y);}const result=document.createElement('canvas');result.width=result.height=32;const rc=result.getContext('2d');rc.imageSmoothingEnabled=false;rc.drawImage(c,l,t,r-l+1,b-t+1,1,2,30,29);return{name,data:result.toDataURL().split(',')[1]};});});for(const o of out)fs.writeFileSync('assets/grass-v25/'+o.name+'.png',Buffer.from(o.data,'base64'));}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1});
