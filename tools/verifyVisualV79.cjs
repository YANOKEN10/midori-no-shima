const fs=require('fs'),http=require('http'),path=require('path'),assert=require('assert/strict');
const root=path.resolve(__dirname,'..'),L=require(root+'/api/_lib'),F=require(root+'/api/_friendStore');
const admin={id:'ヤノケン',created:1789724902905,display:'ヤノケン'},ordinary={id:'tester',created:1};L.setPw(admin,'test-only-password');let docs={};L.readUser=async id=>id===admin.id?admin:id==='tester'?ordinary:null;L.findUser=async name=>name===admin.id?admin:null;L.configured=()=>true;F.read=async keys=>Object.fromEntries(keys.filter(k=>docs[k]).map(k=>[k,structuredClone(docs[k])]));F.commit=async(expected,writes)=>{if(Object.entries(expected).some(([k,v])=>(docs[k]?.version||0)!==v))return false;for(const[k,v]of Object.entries(writes))docs[k]={version:(docs[k]?.version||0)+1,data:structuredClone(v)};return true;};
const handler=require(root+'/api/map-editor'),auth=require(root+'/api/auth');
const mime={'.js':'text/javascript','.mjs':'text/javascript','.json':'application/json','.css':'text/css','.html':'text/html','.png':'image/png','.webp':'image/webp','.mp3':'audio/mpeg'};
const server=http.createServer(async(req,res)=>{res.status=c=>{res.statusCode=c;return res};res.json=v=>{res.setHeader('Content-Type','application/json');res.end(JSON.stringify(v))};const url=new URL(req.url,'http://localhost');if(url.pathname.startsWith('/api/')){let body='';for await(const b of req)body+=b;try{req.body=JSON.parse(body||'{}');}catch{req.body={};}if(url.pathname==='/api/map-editor')return handler(req,res);if(url.pathname==='/api/auth')return auth(req,res);return res.status(200).json({});}let file=path.join(root,decodeURIComponent(url.pathname));if(!file.startsWith(path.normalize(root)))return res.status(403).end();try{if(fs.statSync(file).isDirectory())file=path.join(file,'index.html');res.setHeader('Content-Type',mime[path.extname(file)]||'application/octet-stream');fs.createReadStream(file).pipe(res);}catch{res.status(404).end();}});
(async()=>{await new Promise(r=>server.listen(5183,'127.0.0.1',r));const {chromium}=require('C:/Users/voraz/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),browser=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});try{const p=await browser.newPage({viewport:{width:960,height:850}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:5183/?v4test=rods');await p.waitForFunction(()=>window.VM);await p.waitForTimeout(1200);const data=await p.evaluate(async()=>{const a=await import('/src/npcArt.js'),{heroFrame}=await import('/src/revampArt.js?v=20260913-fashion-v50');const cv=document.createElement('canvas');cv.width=7*128;cv.height=230;const c=cv.getContext('2d');c.fillStyle='#deead8';c.fillRect(0,0,cv.width,cv.height);c.imageSmoothingEnabled=false;const entries=[['主人公',heroFrame('down',1,VM.State.save.look)],['町の人',a.npcFrame({variant:0,dir:'down'},1)],['ヤノケン',a.npcFrame({name:'ヤノケン',dir:'down'},1)],...['大人','おばあさん','子供','おじいさん'].map((n,i)=>[n,a.npcFrame({variant:30+i,dir:'down'},1)])];entries.forEach(([name,im],i)=>{if(!im)throw Error('missing sprite');c.drawImage(im,i*128,0,128,192);c.fillStyle='#24433c';c.font='17px sans-serif';c.fillText(name,i*128+12,220);});return cv.toDataURL().split(',')[1];});fs.writeFileSync('work/admin72/screens/people79.png',Buffer.from(data,'base64'));
await p.evaluate(async()=>{const env=await import('/src/realtimeEnvironment68.js');const cv=document.createElement('canvas');cv.width=320;cv.height=288;const c=cv.getContext('2d'),map={id:'rods',kind:'out',props:[]};c.fillStyle='#a5ce80';c.fillRect(0,0,320,288);env.drawRealtimeEnvironment(c,map,0,new Date('2026-09-19T03:00:00Z'));const day=c.getImageData(160,144,1,1).data;c.fillStyle='#a5ce80';c.fillRect(0,0,320,288);env.drawRealtimeEnvironment(c,map,0,new Date('2026-09-19T13:00:00Z'));const night=c.getImageData(160,144,1,1).data;if(night[0]+night[1]+night[2]>(day[0]+day[1]+day[2])*.7)throw Error('Night too bright');});
await p.evaluate(async()=>{const {ui}=await import('/src/ui.js'),b=await import('/src/battle.js');window.ui79=ui;window.messages79=[];const orig=ui.say.bind(ui);ui.say=async lines=>{window.messages79.push(lines.join(' '));return orig(lines);};window.b79=b;VM.State.save.party=[VM.makeMon('コケゴロ',8)];window.battleRun79=b.startBattle({trainer:{name:'おばあさんの ハナ',appearance79:{variant:31},party:[['コケゴロ',6]],money:50}});});await p.waitForFunction(()=>messages79.length===1);await p.waitForTimeout(900);await p.screenshot({path:'work/admin72/screens/trainer-intro79.png'});for(let i=0;i<7;i++){await p.keyboard.press('z');await p.waitForTimeout(170);if(await p.evaluate(()=>messages79.length>=2))break;}await p.waitForFunction(()=>messages79.length>=2);assert((await p.evaluate(()=>messages79[1])).includes('コケゴロ'));await p.screenshot({path:'work/admin72/screens/trainer-send79.png'});assert.deepEqual(errors,[]);console.log('PASS trainer/name first, then Gaon send-out; night luminance; human proportions preview');}finally{await browser.close();}server.close();})().catch(e=>{console.error(e);server.close();process.exitCode=1});
