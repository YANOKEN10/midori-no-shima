const {chromium}=require('C:/Users/voraz/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const fs=require('node:fs'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});try{const p=await b.newPage();await p.goto(process.argv[2]||'http://127.0.0.1:5182/',{waitUntil:'networkidle'});const result=await p.evaluate(async()=>{const{drawBiomeGrass,grassReady}=await import('/src/grassArt.js');const map={id:'route1',rows:['"']};while(!grassReady(map))await new Promise(r=>setTimeout(r,50));const render=(clip)=>{const cv=document.createElement('canvas');cv.width=cv.height=96;const c=cv.getContext('2d');if(!clip)c.clip=()=>{};drawBiomeGrass(c,map,32,32,0,0);return cv;};const actual=render(true),full=render(false);const a=actual.getContext('2d').getImageData(0,0,96,96).data,f=full.getContext('2d').getImageData(0,0,96,96).data;let lost=0,painted=0;for(let i=3;i<a.length;i+=4){if(f[i]>a[i])lost++;if(a[i])painted++;}const preview=document.createElement('canvas');preview.width=384;preview.height=320;const c=preview.getContext('2d');c.fillStyle='#75c799';c.fillRect(0,0,384,320);c.imageSmoothingEnabled=false;const patch=document.createElement('canvas');patch.width=96;patch.height=80;const pc=patch.getContext('2d');const m={id:'route1',rows:['"""','"""']};for(let y=0;y<2;y++)for(let x=0;x<3;x++)drawBiomeGrass(pc,m,x*32,y*32);c.drawImage(patch,0,0,384,320);return{lost,painted,image:preview.toDataURL()};});fs.writeFileSync('artifacts/grass-edge-v44.png',Buffer.from(result.image.split(',')[1],'base64'));delete result.image;assert.equal(result.lost,0,'Grass pixels lost to tile clipping');assert.ok(result.painted>100);console.log(JSON.stringify(result));}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1});
