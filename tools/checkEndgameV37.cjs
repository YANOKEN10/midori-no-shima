const{chromium}=require('C:/Users/voraz/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const fs=require('fs');
(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});try{const p=await b.newPage();p.on('pageerror',e=>console.log('PAGE',e.message));await p.goto('http://127.0.0.1:5181',{waitUntil:'domcontentloaded'});const report=await p.evaluate(async()=>{const{MAPS}=await import('/src/data/maps.js'),{solid}=await import('/src/tiles.js'),{SPECIES}=await import('/src/data/species.js');const errors=[];for(const[id,m]of Object.entries(MAPS)){const q=[[m.spawn.x,m.spawn.y]],seen=new Set();for(let i=0;i<q.length;i++){const[x,y]=q[i],k=x+','+y;if(seen.has(k)||m.rows[y]?.[x]==null||solid(m.rows[y][x]))continue;seen.add(k);q.push([x+1,y],[x-1,y],[x,y+1],[x,y-1]);}for(const wp of m.warps){if(!seen.has(wp.x+','+wp.y))errors.push('EXIT '+id+' '+wp.x+','+wp.y);if(MAPS[wp.to]&&(MAPS[wp.to].rows[wp.ty]?.[wp.tx]==null||solid(MAPS[wp.to].rows[wp.ty][wp.tx])))errors.push('ARRIVAL '+id+' -> '+wp.to+' '+wp.tx+','+wp.ty);}if(m.endTheme)for(const n of m.npcs){if(solid(m.rows[n.y]?.[n.x]))errors.push('SOLID '+id+' '+n.name+' '+n.x+','+n.y);if(![[1,0],[-1,0],[0,1],[0,-1]].some(([dx,dy])=>seen.has((n.x+dx)+','+(n.y+dy))))errors.push('NPC '+id+' '+n.name+' '+n.x+','+n.y);}for(const e of m.enc?.list||[])if(!SPECIES[e[0]])errors.push('SPECIES '+e[0]);}return {maps:Object.keys(MAPS).length,errors};});console.log(report);if(report.errors.length)throw Error("Map structure failed");}finally{await b.close()}})().catch(e=>{console.error(e);process.exitCode=1});
