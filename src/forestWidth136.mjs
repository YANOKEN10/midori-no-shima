export function upgradeForestWidth136(base,d){if(base.id!=='mountain'||d.forestWidth136)return d;const h=base.rows.length,w=base.rows[0].length,objects=d.objects.flatMap(o=>{if(!o.id.startsWith('a:forest-spacing135-')||o.stored79||o.turn81||o.color115)return [o];const match=o.id.match(/^a:forest-spacing135-(\d+)-(-?\d+)$/);if(!match||o.x!==Number(match[1])||o.y!==Number(match[2])||![ -1,h-2].includes(o.y)||o.x<15)return [o];return o.x+3>w?[]:[{...o,x:o.x+1}];}),tiles=[...d.tiles];for(const y of [0,1,2,h-3,h-2,h-1])if(!tiles.some(t=>t.x===15&&t.y===y))tiles.push({x:15,y,material:'path'});return {...d,forestWidth136:true,objects,tiles};}
