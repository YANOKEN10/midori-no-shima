// Older workshop grass props are floor paint, not solid overlapping objects.
export function upgradeGrassFloor179(base,d){const grass=d.objects.filter(o=>!o.stored79&&o.type==='prop'&&o.id.startsWith('a:')&&o.template==='legacy73-grass');if(!grass.length)return d;const cells=new Set(grass.map(o=>o.x+','+o.y)),ids=new Set(grass.map(o=>o.id)),tiles=[...d.tiles];for(const o of grass)if(!tiles.some(t=>t.x===o.x&&t.y===o.y))tiles.push({x:o.x,y:o.y,material:'legacy73-grass',...(o.color115?{color115:o.color115}:{})});return{...d,tiles,objects:d.objects.map(o=>ids.has(o.id)||o.id.startsWith('g:')&&cells.has(o.x+','+o.y)&&o.template==='wall79'?{...o,stored79:true}:o)};}
